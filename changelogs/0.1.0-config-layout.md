# 0.1.0 Config Layout

- Moved root TypeScript, Vite, Vitest, and Playwright configuration files into `config/`.
- Updated npm scripts and documentation so project commands continue to run from the repository root.
