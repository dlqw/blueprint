# 0.1.0 Debug Launcher

- Added `scripts/debug-launcher.py`, a standard-library Python TUI for starting common Blueprint IDE development, validation, build, packaged-check, and setup commands from the repository root.
- Added non-interactive launcher options for listing actions, running a specific action key, and dry-running the resolved command.
